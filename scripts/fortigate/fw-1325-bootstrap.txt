! Unico paso realizado por CLI (todo lo demas se configuro por GUI):
config system interface
    edit "port1"
        set mode static
        set ip 192.168.139.25 255.255.255.0
        set allowaccess ping https ssh http
    next
end
